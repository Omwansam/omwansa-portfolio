{
  "name": "omwansa-portfolio-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install-client": "npm ci --prefix client/my-portfolio",
    "build": "npm run --prefix client/my-portfolio build",
    "postinstall": "npm run install-client && npm run build"
  },
  "engines": {
    "node": ">=18"
  }
}


